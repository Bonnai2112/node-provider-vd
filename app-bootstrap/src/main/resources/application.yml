spring:
  application:
    name: node-provider
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:nodeprovider}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

app:
  platform:
    eth-docker:
      root-dir: ${PLATFORM_ROOT:/tmp/platform/nodes}
      cache-dir: ${PLATFORM_CACHE:/tmp/platform/cache}
      sha-cache-file: ${PLATFORM_SHA_CACHE:/tmp/platform/cache/eth-docker-sha}

logging:
  level:
    com.ceticgroup.cloud.nodeprovider: INFO
